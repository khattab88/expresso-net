@model Models.Tag

@{
    ViewBag.Title = "New";
}

<div class="row">
    <div class="col">
        <div class="app-page-title mb-0 py-2">
            <div class="page-title-wrapper">
                <div class="page-title-heading">
                    <h1>New Tag</h1>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-lg-6">
        <div class="main-card mb-3 mt-3 card">
            <div class="card-body">

                @*<form method="post" action="tags">
                    <input id="id" type="hidden" name="id" />
                    <div class="position-relative form-group">
                        <label for="name">Name</label>
                        <input id="name" class="form-control" name="name" placeholder="Name" required />
                    </div>
                    <div class="d-flex justify-content-end pt-3">
                        <button class="mt-1 mr-2 btn btn-success">Submit</button>
                        @Html.ActionLink("Cancel", "Index", "Tags", null, new { @class = "mt-1 btn btn-danger" })
                    </div>
                </form>*@

                @using (@Html.BeginForm("Create", "Tags"))
                {
                    <input id="id" type="hidden" name="id" value="0" />
                    <div class="position-relative form-group">
                        @Html.LabelFor(m => m.Name)
                        @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "Name", required = true })
                    </div>
                }
            </div>
        </div>
    </div>
</div>